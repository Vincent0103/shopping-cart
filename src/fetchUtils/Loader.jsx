const Loader = () => (
  <div className="size-full bg-transparent backdrop-blur-xl">
    <div className="w-full h-[80vh] flex items-center justify-center">
      <h1 className="rotate-1 text-[10vw] font-black">LOADING...</h1>
    </div>
  </div>
);

export default Loader;
